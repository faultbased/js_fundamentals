const reapperArray = ['lil\' pump', 'Lil\' Xan', 'Lil\' Peep'];

for (let i = 0; i < reapperArray.length; i++) {
	console.log(reapperArray[i]);
		if (i === 2) {
			break;
		}
};

console.log("And... life finds a way to make things worse.");