//anon arrow function
console.info(() => console.info('sugar, honey..'));
//defining a named function from arrow (function) expression and saved to helloWorld variable
const helloWorld = (input) => {
    console.info(`Welcome ${input} to your... dreamscape, this is what you always wanted`);
};

helloWorld('Gunther');

